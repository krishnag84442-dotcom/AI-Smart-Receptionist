services:
  - type: web
    name: ai-smart-receptionist-backend
    env: python3
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && python start.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
